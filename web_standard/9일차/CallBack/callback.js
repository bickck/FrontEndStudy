

class UserStorage {

    loginUser(id,password,onSuccess,onError){
        setTimeout(()=> {
            if(
                (id ===)
            )
        },2000);
    }

    getRoles(user,onSuccess,onError){

    }
}